import 'package:flutter/material.dart';

class NoteTimelineItems extends StatelessWidget {
  const NoteTimelineItems({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Nothing note, yet..."),
    );
  }
}
